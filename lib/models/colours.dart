import 'package:flutter/material.dart';

class AppColour {
  // Background
  static const Color background = Color(0xFFF5FDF6);

  // Primary Green (for buttons or highlights)
  static const Color primaryGreen = Color(0xFF6BBF59);

  static const Color secondaryGreen = Color(0xFFBCEBCB);
}
